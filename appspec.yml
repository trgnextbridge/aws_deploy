version: 0.3
os: linux
phases:
  install:
    runtime-versions:
        php: 7.3
files:
    - source: /
      destination: /var/www/html
permissions:
    - object: /var/www/html
      pattern: "**"
      owner: root
      group: root
      mode: 755
      type:
        - file

